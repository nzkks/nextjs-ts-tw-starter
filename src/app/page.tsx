import { ThemeSwitch } from '@/components';
import { Button } from '@/components/ui/button';

const Home = () => {
  return (
    <main>
      <ThemeSwitch />
      <p>Hello</p>
      <Button>Button</Button>
    </main>
  );
};

export default Home;
